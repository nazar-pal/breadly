import { AccountSelectSQLite } from '@/powersync/schema/table_6_accounts'

// Mock data remains for reference, but won't be used in the updated components
export const mockAccountsByType = {
  payment: [] as AccountSelectSQLite[],
  saving: [] as AccountSelectSQLite[],
  debt: [] as AccountSelectSQLite[]
}
