interface PaymentFieldsProps {
  control: any // Not used but kept for consistency
}

export default function PaymentFields(_props: PaymentFieldsProps) {
  // Payment accounts have no additional fields beyond the common ones
  return null
}
